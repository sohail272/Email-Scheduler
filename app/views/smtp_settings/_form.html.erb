<%= form_with(model: smtp_setting, local: true) do |form| %>
  <% if smtp_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(smtp_setting.errors.count, "error") %> prohibited this smtp_setting from being saved:</h2>

      <ul>
      <% smtp_setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :address%>
    <%= form.text_field :address %> </br>

    <%= form.label :port%>
    <%= form.text_field :port %> </br>

    <%= form.label :username%>
    <%= form.text_field :username %> </br>

    <%= form.label :password%>
    <%= form.password_field :password %> </br>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
